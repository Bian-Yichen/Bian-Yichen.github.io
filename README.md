# Academic Homepage

Minimal, responsive academic homepage served at `https://bian-yichen.github.io/`.

Edit the bracketed placeholder text in `index.html` and replace the `#` links with your own URLs. Replace the `.portrait-placeholder` block with an `<img>` element when adding a profile photo.
